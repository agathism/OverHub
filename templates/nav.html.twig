<nav class=" text-black flex justify-center mb-0 my-4 sm:flex-row">
  <ul class="flex justify-center text-xl">
    <li class="list-none">
      <a class=" translate-all block py-4 px-5 list-none" href="{{path('homepage')}}">Home</a>
    </li>
    <li class="list-none">
      <a class=" translate-all block py-4 px-5 list-none" href="{{path('app_heroes')}}">Heroes</a>
    </li>
    <li class="list-none">
      <a class=" translate-all block py-4 px-5 list-none" href="{{path('app_contact')}}">Contact</a>
    </li>
    <li class="list-none">
      <a class=" translate-all block py-4 px-5 list-none" href="{{path('app_login')}}">Login</a>
    </li>
    {# {% if is_granted('ROLE_ADMIN') %}
    <li>
      <a class=" translate-all block py-4 px-5 list-none" href="{{path('app_profile')}}">Profile</a>
    </li>
    <li>
      <a class=" translate-all block py-4 px-5 list-none" href="{{path('app_logout')}}">Déconnexion</a>
    </li>
    {% else %}
    <li>
      <a class=" translate-all block py-4 px-5 list-none" href="{{path('app_login')}}">Login</a>
    </li>
    {% endif %} #}
</nav>